<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:gravity="center"
    android:orientation="vertical"
    android:id="@+id/llayout_error_hint"
    android:background="@drawable/bg_palybegin"
    android:clickable="true"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <ImageView
        android:id="@+id/iv_error_services"
        android:visibility="visible"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:src="@drawable/player_error"/>
    <TextView
        android:textSize="16dp"
        android:textColor="@color/WHITE"
        android:layout_gravity="center_horizontal"
        android:id="@+id/txtvew_error_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"/>
    <TextView
        android:textSize="16dp"
        android:textColor="@color/white"
        android:gravity="center_vertical"
        android:layout_gravity="center"
        android:id="@+id/txt_error"
        android:background="@drawable/player_btn_tip_bg"
        android:paddingTop="5dp"
        android:paddingBottom="5dp"
        android:clickable="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"/>
</LinearLayout>
