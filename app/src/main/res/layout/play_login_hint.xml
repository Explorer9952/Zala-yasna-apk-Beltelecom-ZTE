<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:gravity="center"
    android:orientation="vertical"
    android:id="@+id/llayout_login_hint"
    android:background="@color/keyboard_bg_color"
    android:visibility="gone"
    android:clickable="true"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <TextView
        android:textSize="16dp"
        android:textColor="@color/WHITE"
        android:gravity="center_horizontal"
        android:layout_gravity="center_horizontal"
        android:id="@+id/txtvew_login_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"/>
    <LinearLayout
        android:gravity="center"
        android:id="@+id/ll_hint_login"
        android:clickable="true"
        android:layout_width="200dp"
        android:layout_height="35dp"
        android:layout_marginTop="10dp">
        <Button
            android:textSize="16dp"
            android:textColor="@color/text_red_new3"
            android:layout_gravity="center"
            android:id="@+id/btn_login"
            android:background="@drawable/bg_palybegin_contiune4g"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAllCaps="false"
            android:paddingStart="3dp"
            android:paddingEnd="3dp"/>
    </LinearLayout>
</LinearLayout>
